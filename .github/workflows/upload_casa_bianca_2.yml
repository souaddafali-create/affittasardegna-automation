name: CaseVacanza - Casa Bianca 2

on:
  push:
    branches: [main]
    paths:
      - 'Casa_Bianca_2_DATI.json'
      - 'casevacanza_uploader.py'
  workflow_dispatch:

concurrency:
  group: casevacanza-global
  cancel-in-progress: false

jobs:
  upload:
    runs-on: ubuntu-latest
    environment: Default

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-chromium

      - name: Install Playwright
        run: |
          pip install playwright Pillow
          playwright install chromium --with-deps

      - name: Run uploader (Casa Bianca 2)
        env:
          CASEVACANZA_EMAIL: ${{ secrets.CASEVACANZA_EMAIL }}
          CASEVACANZA_PASSWORD: ${{ secrets.CASEVACANZA_PASSWORD }}
          PROPERTY_DATA: Casa_Bianca_2_DATI.json
        run: xvfb-run python casevacanza_uploader.py

      - name: Upload debug artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: casa-bianca-2-debug-screenshots
          path: screenshots/
